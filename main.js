/* ==============================================================
// GLOBAL TRANSLATION DICTIONARY (9 LANGUAGES)
// ============================================================== */
window.dict = {
    'EN': { subtitle: "Architectural Core Intelligence", login: "Login", email: "EMAIL ADDRESS", password: "PASSWORD", orLogin: "Or Login With", warning: "ADEUL AI IS AN ARTIFICIAL INTELLIGENCE MODEL AND CAN MAKE MISTAKES. PLEASE VERIFY RESULTS.", credits: "REMAINING CREDITS", add: "+ ADD", workspace: "WORKSPACE", dashboard_menu: "DASHBOARD", projects: "PROJECTS", renders: "RENDERS", assets: "ASSETS", workflows: "WORKFLOWS", feed: "FEED", account: "ACCOUNT & SETTINGS", settings: "SETTINGS", billing: "BILLING", invite: "INVITE TEAM", myaccount: "MY ACCOUNT", support: "SUPPORT", interior: "INTERIOR", exterior: "EXTERIOR", architecture: "ARCHITECTURE", design: "DESIGN", planResize: "PLAN RESIZE", presentation: "PRESENTATION", freetrial: "Start Free Trial", uploadScene: "UPLOAD SCENE", uploadItem: "UPLOAD ITEM", uploadRef: "UPLOAD REFERENCE", uploadDesign: "PRODUCT PLACEMENT & REVISION", uploadSketch: "UPLOAD SKETCH", uploadMax: "UPLOAD MAX VIEWPORT", uploadPlan: "UPLOAD PNG / JPG PLAN", writePrompt: "WRITE PROMPT", promptPh: "Describe your architectural vision...", generateNormal: "NORMAL RENDER", generate8k: "8K RENDER", generate: "GENERATE RENDER", revisePh: "REVISION...", update: "SAVE TO DEVICE", vendorNetwork: "VENDOR NETWORK", architect: "ARCHITECT", contractor: "CONTRACTOR", electrician: "ELECTRICIAN", hvac: "HVAC / MECHANICAL", plumber: "PLUMBER", metal: "METAL WORKSHOP", upholstery: "UPHOLSTERY", painter: "PAINTER", construction: "CONSTRUCTION", carpenter: "CARPENTER", furniture: "FURNITURE STORE", getQuote: "GET QUOTE", board1: "BOARD 1", board2: "BOARD 2", board3: "BOARD 3", round: "ROUND", square: "SQUARE", schema: "SCHEMA", downloadBoard: "DOWNLOAD BOARD", putProduct: "UPLOAD PRODUCT", createTexture: "GENERATE MATERIAL BOARD", createPresentation: "CREATE PRESENTATION" },
    'TR': { subtitle: "Mimari Çekirdek Zekası", login: "Giriş Yap", email: "E-POSTA ADRESİ", password: "ŞİFRE", orLogin: "Veya Şununla Giriş Yap", warning: "ADEUL AI YAPAY ZEKA MODELİDİR HATA YAPABİLİR. LÜTFEN SONUÇLARI DOĞRULAYIN.", credits: "KALAN KREDİ", add: "+ YÜKLE", workspace: "ÇALIŞMA ALANI", dashboard_menu: "PANEL", projects: "PROJELER", renders: "RENDERLAR", assets: "VARLIKLAR", workflows: "İŞ AKIŞLARI", feed: "AKIŞ", account: "HESAP & AYARLAR", settings: "AYARLAR", billing: "FATURALANDIRMA", invite: "EKİP DAVET ET", myaccount: "HESABIM", support: "DESTEK", interior: "İÇ MEKAN", exterior: "DIŞ MEKAN", architecture: "MİMARİ", design: "TASARIM", planResize: "PLAN BOYUTLANDIRMA", presentation: "SUNUM", freetrial: "Ücretsiz Denemeye Başla", uploadScene: "SAHNE YÜKLE", uploadItem: "EŞYA YÜKLE", uploadRef: "REFERANS YÜKLE", uploadDesign: "ÜRÜN YERLEŞTİRME & REVİZYON", uploadSketch: "ESKİZ YÜKLE", uploadMax: "MAX GÖRÜNTÜSÜ YÜKLE", uploadPlan: "PNG / JPG PLAN YÜKLE", writePrompt: "PROMPT YAZ", promptPh: "Mimari vizyonunuzu tarif edin...", generateNormal: "NORMAL RENDER", generate8k: "8K RENDER", generate: "RENDER AL", revisePh: "REVİZYON...", update: "CİHAZA KAYDET", vendorNetwork: "ÜRETİCİ AĞI", architect: "MİMAR", contractor: "MÜTEAHHİT", electrician: "ELEKTRİKÇİ", hvac: "MEKANİK / İKLİMLENDİRME", plumber: "TESİSATÇI", metal: "METAL ATÖLYESİ", upholstery: "DÖŞEMECİ", painter: "BOYACI", construction: "İNŞAAT", carpenter: "MARANGOZ", furniture: "MOBİLYA MAĞAZASI", getQuote: "FİYAT AL", board1: "BORD 1", board2: "BORD 2", board3: "BORD 3", round: "YUVARLAK", square: "KARE", schema: "ŞEMA", downloadBoard: "PAFTAYI İNDİR", putProduct: "ÜRÜN KOY", createTexture: "MALZEME RENDERI OLUŞTUR", createPresentation: "SUNUM OLUŞTUR" },
    'ES': { subtitle: "Inteligencia Central Arquitectónica", login: "Iniciar Sesión", email: "CORREO ELECTRÓNICO", password: "CONTRASEÑA", orLogin: "O entrar con", warning: "ADEUL AI PUEDE COMETER ERRORES. VERIFIQUE LOS RESULTADOS.", credits: "CRÉDITOS RESTANTES", add: "+ AÑADIR", workspace: "ESPACE DE TRABAJO", dashboard_menu: "TABLERO", projects: "PROJETS", renders: "RENDERS", assets: "ACTIVOS", workflows: "FLUJOS", feed: "FEED", account: "CUENTA Y AJUSTES", settings: "AJUSTES", billing: "FACTURACIÓN", invite: "INVITAR EQUIPO", myaccount: "MI CUENTA", support: "SOPORTE", interior: "INTERIOR", exterior: "EXTERIOR", architecture: "ARQUITECTURA", design: "DISEÑO", planResize: "REDIMENSIONAR PLANO", presentation: "PRESENTACIÓN", freetrial: "Prueba Gratis", uploadScene: "SUBIR ESCENA", uploadItem: "SUBIR OBJETO", uploadRef: "REFERENCIA", uploadDesign: "PRODUCCIÓN Y REVISIÓN", uploadSketch: "SUBIR BOCETO", uploadMax: "SUBIR VISTA MAX", uploadPlan: "SUBIR PLANO PNG / JPG", writePrompt: "ESCRIBIR PROMPT", promptPh: "Describe tu visión arquitectónica...", generateNormal: "RENDER NORMAL", generate8k: "RENDER 8K", generate: "GENERAR RENDER", revisePh: "REVISIÓN...", update: "GUARDAR EN EL DISPOSITIVO", vendorNetwork: "RED DE VENDEDORES", architect: "ARQUITECTO", contractor: "CONTRATISTA", electrician: "ELECTRICISTA", hvac: "CLIMATIZACIÓN", plumber: "FONTANERO", metal: "TALLER METAL", upholstery: "TAPICERÍA", painter: "PINTOR", construction: "CONSTRUCTION", carpenter: "CARPINTERO", furniture: "MUEBLERÍA", getQuote: "COTIZAR", board1: "TABLERO 1", board2: "TABLERO 2", board3: "TABLERO 3", round: "REDONDO", square: "CUADRADO", schema: "ESQUEMA", downloadBoard: "DESCARGAR BORD", putProduct: "CARGAR PRODUCTO", createTexture: "RENDER DE MATERIALES", createPresentation: "CREAR PRESENTACIÓN" },
    'DE': { subtitle: "Architektonische Kernintelligenz", login: "Anmelden", email: "E-MAIL", password: "PASSWORT", orLogin: "Oder anmelden mit", warning: "ADEUL AI KANN FEHLER MACHEN. BITTE ERGEBNISSE ÜBERPRÜFEN.", credits: "VERBLEIBENDE KREDITE", add: "+ HINZUFÜGEN", workspace: "ARBEITSBEREICH", dashboard_menu: "DASHBOARD", projects: "PROJEKTE", renders: "RENDERS", assets: "ASSETS", workflows: "WORKFLOWS", feed: "FEED", account: "KONTO & EINSTELLUNGEN", settings: "EINSTELLUNGEN", billing: "ABRECHNUNG", invite: "TEAM EINLADEN", myaccount: "MEIN KONTO", support: "SUPPORT", interior: "INTERIEUR", exterior: "EXTERIEUR", architecture: "ARCHITEKTUR", design: "DESIGN", planResize: "PLANGRÖSSE ÄNDERN", presentation: "PRÄSENTATION", freetrial: "Kostenlos Testen", uploadScene: "SZENE HOCHLADEN", uploadItem: "OBJEKT HOCHLADEN", uploadRef: "REFERENZ", uploadDesign: "PRODUKTION & REVISION", uploadSketch: "SKIZZE HOCHLADEN", uploadMax: "MAX-ANSICHT", uploadPlan: "PNG / JPG PLAN HOCHLADEN", writePrompt: "PROMPT SCHREIBEN", promptPh: "Beschreiben Sie Ihre Vision...", generateNormal: "NORMALER RENDER", generate8k: "8K RENDER", generate: "RENDER GENERIEREN", revisePh: "REVISION...", update: "AUF GERÄT SPEICHERN", vendorNetwork: "HÄNDLERNETZWERK", architect: "ARCHITEKT", contractor: "BAUNTERNEHMER", electrician: "ELEKTRIKER", hvac: "KLIMAANLAGE", plumber: "INSTALLATEUR", metal: "METALLWERKSTATT", upholstery: "POLSTEREI", painter: "MALER", construction: "BAU", carpenter: "ZIMMERMANN", furniture: "MÖBELHAUS", getQuote: "ANGEBOT", board1: "BOARD 1", board2: "BOARD 2", board3: "BOARD 3", round: "RUND", square: "QUADRAT", schema: "SCHEMA", downloadBoard: "BOARD HERUNTERLADEN", putProduct: "PRODUKT LADEN", createTexture: "MATERIALIEN ERSTELLEN", createPresentation: "PRÄSENTATION ERSTELLEN" },
    'FR': { subtitle: "Intelligence Architecturale", login: "Connexion", email: "E-MAIL", password: "MOT DE PASSE", orLogin: "Ou se connecter avec", warning: "L'IA PEUT FAIRE DES ERREURS. VEUILLEZ VÉRIFIER LES RÉSULTATS.", credits: "CRÉDITOS RESTANTS", add: "+ AJOUTER", workspace: "ESPACE DE TRAVAIL", dashboard_menu: "TABLEAU DE BORD", projects: "PROJETS", renders: "RENDUS", assets: "ASSETS", workflows: "FLUX", feed: "FLUX", account: "COMPTE & PARAMÈTRES", settings: "PARAMÈTRES", billing: "FACTURATION", invite: "INVITER ÉQUIPE", myaccount: "MON COMPTE", support: "SUPPORT", interior: "INTÉRIEUR", exterior: "EXTÉRIEUR", architecture: "ARCHITECTURE", design: "DESIGN", planResize: "REDIMENSIONNER PLAN", presentation: "PRÉSENTATION", freetrial: "Essai Gratuit", uploadScene: "CHARGER SCÈNE", uploadItem: "CHARGER OBJET", uploadRef: "RÉFÉRENCE", uploadDesign: "PRODUCTION ET RÉVISION", uploadSketch: "CHARGER CROQUIS", uploadMax: "CHARGER VUE MAX", uploadPlan: "CHARGER PLAN PNG / JPG", writePrompt: "ÉCRIRE PROMPT", promptPh: "Décrivez votre vision...", generateNormal: "RENDU NORMAL", generate8k: "RENDU 8K", generate: "GÉNÉRER RENDU", revisePh: "RÉVISION...", update: "ENREGISTRER SUR L'APPAREIL", vendorNetwork: "RÉSEAU VENDEURS", architect: "ARCHITECTE", contractor: "ENTREPRENEUR", electrician: "ÉLECTRICIEN", hvac: "CVC", plumber: "PLOMBIER", metal: "ATELIER MÉTAL", upholstery: "TAPISSERIE", painter: "PEINTRE", construction: "CONSTRUCTION", carpenter: "MENUISIER", furniture: "MAGASIN MEUBLES", getQuote: "DEVIS", board1: "PLANCHE 1", board2: "PLANCHE 2", board3: "PLANCHE 3", round: "ROND", square: "CARRÉ", schema: "SCHÉMA", downloadBoard: "TÉLÉCHARGER PLANCHE", putProduct: "CHARGER PRODUIT", createTexture: "GÉNÉRER MATÉRIAUX", createPresentation: "CRÉER PRÉSENTATION" },
    'PT': { subtitle: "Inteligência Arquitetônica", login: "Entrar", email: "E-MAIL", password: "SENHA", orLogin: "Ou entrar com", warning: "IA PODE COMETER ERROS. VERIFIQUE OS RESULTADOS.", credits: "CRÉDITOS RESTANTES", add: "+ ADICIONAR", workspace: "ESPAÇO DE TRABALHO", dashboard_menu: "PAINEL", projects: "PROJETOS", renders: "RENDERS", assets: "ATIVOS", workflows: "FLUXOS", feed: "FEED", account: "CONTA & CONFIG", settings: "CONFIGURAÇÕES", billing: "FATURAMENTO", invite: "CONVIDAR EQUIPE", myaccount: "MINHA CONTA", support: "SUPORTE", interior: "INTERIOR", exterior: "EXTERIOR", architecture: "ARQUITETURA", design: "DESIGN", planResize: "REDIMENSIONAR PLANO", presentation: "APRESENTAÇÃO", freetrial: "Teste Grátis", uploadScene: "CARREGAR CENA", uploadItem: "CARREGAR ITEM", uploadRef: "CARREGAR REF", uploadDesign: "PRODUÇÃO E REVISÃO", uploadSketch: "CARREGAR ESBOÇO", uploadMax: "CARREGAR VIEWPORT", uploadPlan: "CARREGAR PLANO PNG / JPG", writePrompt: "ESCREVER PROMPT", promptPh: "Descreva sua visão...", generateNormal: "RENDER NORMAL", generate8k: "RENDER 8K", generate: "GERAR RENDER", revisePh: "REVISÃO...", update: "SALVAR NO DISPOSITIVO", vendorNetwork: "REDE DE FORNECEDORES", architect: "ARQUITETO", contractor: "EMPREITEIRO", electrician: "ELETRICISTA", hvac: "AVAC", plumber: "ENCANADOR", metal: "SERRALHERIA", upholstery: "ESTOFARIA", painter: "PINTOR", construction: "CONSTRUÇÃO", carpenter: "MARCENEIRO", furniture: "LOJA MÓVEIS", getQuote: "ORÇAMENTO", board1: "PRANCHA 1", board2: "PRANCHA 2", board3: "PRANCHA 3", round: "REDONDO", square: "QUADRADO", schema: "ESQUEMA", downloadBoard: "BAIXAR PRANCHA", putProduct: "CARREGAR PRODUTO", createTexture: "RENDERIZAR MATERIAIS", createPresentation: "CRIAR APRESENTAÇÃO" },
    'ID': { subtitle: "Kecerdasan Arsitektur", login: "Masuk", email: "EMAIL", password: "KATA SANDI", orLogin: "Atau masuk dengan", warning: "AI BISA MEMBUAT KESALAHAN. VERIFIKASI HASIL.", credits: "SISA KREDIT", add: "+ TAMBAH", workspace: "RUANG KERJA", dashboard_menu: "DASHBOARD", projects: "PROYEK", renders: "RENDER", assets: "ASET", workflows: "ALUR KERJA", feed: "FEED", account: "AKUN & PENGATURAN", settings: "PENGATURAN", billing: "TAGIHAN", invite: "UNDANG TIM", myaccount: "AKUN SAYA", support: "DUKUNGAN", interior: "INTERIOR", exterior: "EKSTERIOR", architecture: "ARSITEKTUR", design: "DESAIN", planResize: "UBAH UKURAN DENAH", presentation: "PRESENTASI", freetrial: "Coba Gratis", uploadScene: "UNGGAH SCENE", uploadItem: "UNGGAH ITEM", uploadRef: "UNGGAH REF", uploadDesign: "PRODUKSI & REVISI", uploadSketch: "UNGGAH SKETSA", uploadMax: "UNGGAH VIEWPORT", uploadPlan: "UNGGAH DENAH PNG / JPG", writePrompt: "TULIS PROMPT", promptPh: "Jelaskan visi Anda...", generateNormal: "RENDER NORMAL", generate8k: "RENDER 8K", generate: "BUAT RENDER", revisePh: "REVISI...", update: "SIMPAN KE PERANGKAT", vendorNetwork: "JARINGAN VENDOR", architect: "ARSITEK", contractor: "KONTRAKTOR", electrician: "TEKNISI LISTRIK", hvac: "HVAC", plumber: "TUKANG PIPA", metal: "BENGKEL LOGAM", upholstery: "JOK KURSI", painter: "TUKANG CAT", construction: "KONSTRUKSI", carpenter: "TUKANG KAYU", furniture: "TOKO MEBEL", getQuote: "PENAWARAN", board1: "PAPAN 1", board2: "PAPAN 2", board3: "PAPAN 3", round: "BULAT", square: "PERSEGI", schema: "SKEMA", downloadBoard: "UNDUH PAPAN", putProduct: "UNGGAH PRODUK", createTexture: "BUAT MATERIAL RENDER", createPresentation: "BUAT PRESENTASI" },
    'HI': { subtitle: "वास्तु इंटेलिजेंस", login: "लॉग इन करें", email: "ईमेल", password: "पासवर्ड", orLogin: "या इसके साथ लॉग input करें", warning: "AI गलतियाँ कर सकता है। परिणामों की पुष्टि करें।", credits: "शेष क्रेडिट", add: "+ जोड़ें", workspace: "कार्यक्षेत्र", dashboard_menu: "डैशबोर्ड", projects: "परियोजनाएं", renders: "रेंडर", assets: "संपत्ति", workflows: "कार्यप्रवाह", feed: "फ़ीड", account: "खाता और सेटिंग्स", settings: "सेटिंग्स", billing: "बिलिंग", invite: "टीम आमंत्रित करें", myaccount: "मेरा खाता", support: "समर्थन", interior: "आंतरिक", exterior: "बाहरी", architecture: "वास्तुकला", design: "डिज़ाइन", planResize: "प्लान का आकार बदलें", presentation: "प्रस्तुति", freetrial: "निःशुल्क परीक्षण", uploadScene: " दृश्य अपलोड करें", uploadItem: "आइटम अपलोड करें", uploadRef: "संदर्भ अपलोड करें", uploadDesign: "उत्पादन और संशोधन", uploadSketch: "स्केच अपलोड करें", uploadMax: "मैक्स व्यू अपलोड करें", uploadPlan: "PNG / JPG प्लान अपलोड करें", writePrompt: "प्रॉम्प्ट लिखें", promptPh: "अपनी वास्तु दृष्टि का वर्णन करें...", generateNormal: "सामान्य रेंडर", generate8k: "8K रेंडर", generate: "रेंडर जनरेट करें", revisePh: "संशोधन...", update: "डिवाइस में सहेजें", vendorNetwork: "विक्रेता नेटवर्क", architect: "वास्तुकार", contractor: "ठेकेदार", electrician: "इलेक्ट्रीशियन", hvac: "एचवीएसी", plumber: "प्लम्बर", metal: "धातु कार्यशाला", upholstery: "असबाब", painter: "पेंटर", construction: "निर्माण", carpenter: "बढ़ई", furniture: "फर्नीचर स्टोर", getQuote: "उद्धरण प्राप्त करें", board1: "बोर्ड 1", board2: "बोर्ड 2", board3: "बोर्ड 3", round: "गोल", square: "वर्ग", schema: "योजना", downloadBoard: "डाउनलोड करें", putProduct: "उत्पाद अपलोड करें", createTexture: "सामग्री रेंडर बनाएं", createPresentation: "प्रस्तुति बनाएं" },
    'AR': { subtitle: "الذكاء المعماري", login: "دخول", email: "البريد الإلكتروني", password: "كلمة المرور", orLogin: "أو الدخول عبر", warning: "قد يخطئ الذكاء الاصطناعي. تحقق من النتائج.", credits: "الرصيد المتبقي", add: "+ إضافة", workspace: "مساحة العمل", dashboard_menu: "لوحة القيادة", projects: "المشاريع", renders: "التصاميم", assets: "الأصول", workflows: "سير العمل", feed: "الموجز", account: "الحساب والإعدادات", settings: "الإعدادات", billing: "الفواتير", invite: "دعوة فريق", myaccount: "حسابي", support: "الدعم", interior: "داخلي", exterior: "خارجي", architecture: "هندسة معمارية", design: "تصميم", planResize: "تغيير حجم المخطط", presentation: "عرض", freetrial: "تجربة مجانية", uploadScene: "تحميل مشهد", uploadItem: "تحميل عنصر", uploadRef: "تحميل مرجع", uploadDesign: "الإنتاج والمراجعة", uploadSketch: "تحميل رسم تخطيطي", uploadMax: "تحميل عرض MAX", uploadPlan: "تحميل مخطط PNG / JPG", writePrompt: "اكتب الوصف", promptPh: "صف رؤيتك المعمارية...", generateNormal: "عرض عادي", generate8k: "عرض عرض 8K", generate: "إنشاء تصميم", revisePh: "مراجعة...", update: "حفظ على الجهاز", vendorNetwork: "شبكة الموردين", architect: "مهندس", contractor: "مقاول", electrician: "كهربائي", hvac: "تكييف", plumber: "سباك", metal: "ورشة معادن", upholstery: "تنجيد", painter: "دهان", construction: "بناء", carpenter: "نجار", furniture: "متجر أثاث", getQuote: "احصل على عرض", board1: "لوحة 1", board2: "لوحة 2", board3: "لوحة 3", round: "دائري", square: "مربع", schema: "مخطط", downloadBoard: "تحميل اللوحة", putProduct: "تحميل المنتج", createTexture: "إنشاء عرض المواد", createPresentation: "إنشاء عرض" }
};

/* ==============================================================
// GLOBAL STATE VARIABLES
// ============================================================== */
window.uploadedBase64 = {}; 
window.currentBoardStyle = 1; 
window.currentRatio = '16:9'; 
window.clickSound = document.getElementById('hoverSound');
window.CLOUDFLARE_URL = "https://adeul-ia.app.n8n.cloud/webhook/adeul-ai-v2";

function playSound() { 
    if(window.clickSound) { 
        window.clickSound.currentTime = 0; 
        window.clickSound.play().catch(e => {}); 
    } 
}

/* ==============================================================
// UI VE "LÜTFEN BEKLEYİN" YÜKLEME EKRANI FONKSİYONLARI
// ============================================================== */

function toggleUpscaleLoader(show) {
    let loader = document.getElementById('adeulUpscaleLoader');
    const container = document.getElementById('renderImgContainer');
    
    if (!loader && container) {
        loader = document.createElement('div');
        loader.id = 'adeulUpscaleLoader';
        loader.className = 'absolute inset-0 bg-black/70 flex flex-col items-center justify-center text-white z-50 backdrop-blur-md transition-opacity duration-300';
        loader.innerHTML = `
            <svg class="animate-spin h-12 w-12 text-blue-500 mb-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            <div class="text-lg font-bold tracking-widest uppercase">8K RENDER ALINIYOR</div>
            <div class="text-sm font-light text-gray-300 mt-2">Lütfen bekleyiniz...</div>
        `;
        container.appendChild(loader);
    }
    
    if (loader) {
        if (show) {
            loader.style.display = 'flex';
            setTimeout(() => loader.style.opacity = '1', 10);
        } else {
            loader.style.opacity = '0';
            setTimeout(() => loader.style.display = 'none', 300);
        }
    }
}

window.toggleLangMenu = function() {
    playSound();
    const langDropdown = document.getElementById('langDropdown');
    if (langDropdown) {
        langDropdown.classList.toggle('hidden');
        langDropdown.classList.toggle('flex');
    }
};

window.selectLang = function(code, flag) {
    playSound();
    
    const activeCodeEl = document.getElementById('activeCode');
    const activeFlagEl = document.getElementById('activeFlag');
    const langDropdown = document.getElementById('langDropdown');

    if (activeCodeEl) activeCodeEl.innerText = code;
    if (activeFlagEl) activeFlagEl.innerText = flag;
    if (langDropdown) {
        langDropdown.classList.add('hidden');
        langDropdown.classList.remove('flex');
    }

    if (window.dict && window.dict[code]) {
        document.querySelectorAll('[data-i18n]').forEach(el => {
            const key = el.getAttribute('data-i18n');
            if (window.dict[code][key]) {
                if (el.innerHTML.includes('<span')) {
                    const spanPart = el.innerHTML.substring(el.innerHTML.indexOf('<span'));
                    el.innerHTML = window.dict[code][key] + " " + spanPart;
                } else {
                    el.innerText = window.dict[code][key];
                }
            }
        });
        
        document.querySelectorAll('[data-i18n-ph]').forEach(el => {
            const key = el.getAttribute('data-i18n-ph');
            if (window.dict[code][key]) el.placeholder = window.dict[code][key];
        });
        
        const dashTitle = document.getElementById('dashboardTitle');
        if (dashTitle) {
            const currentTitleRaw = dashTitle.getAttribute('data-raw-title');
            if (currentTitleRaw && window.dict[code][currentTitleRaw.toLowerCase()]) {
                dashTitle.innerText = window.dict[code][currentTitleRaw.toLowerCase()];
            } else if (currentTitleRaw && currentTitleRaw.toLowerCase() === 'plan') {
                dashTitle.innerText = window.dict[code]['planResize'];
            }
        }
    }
};

window.setRatio = function(ratio, btn) {
    window.currentRatio = ratio;
    playSound();
    document.querySelectorAll('.ratio-btn').forEach(el => {
        el.classList.remove('active', 'bg-white/20', 'text-white');
        el.classList.add('bg-white/5', 'text-white/50');
    });
    btn.classList.remove('bg-white/5', 'text-white/50');
    btn.classList.add('active', 'bg-white/20', 'text-white');
};

document.addEventListener('DOMContentLoaded', () => {
    window.selectLang('EN', '🇺🇸');
});

function handleLogin(e) {
    e.preventDefault();
    playSound();
    document.getElementById('loginScreen').style.opacity = '0';
    setTimeout(() => {
        document.getElementById('loginScreen').style.display = 'none';
        document.getElementById('appContent').classList.add('active');
    }, 800);
}

function toggleUserMenu() {
    playSound();
    document.getElementById('userSidePanel').classList.toggle('open');
}

// 🔥 KUSURSUZ YÜKLEME: Canvas İptal, Saf Base64 Aktarımı
function previewImage(input, boxId) {
    playSound();
    if (input.files && input.files[0]) {
        const reader = new FileReader();
        reader.onload = function(e) {
            const rawDataUrl = e.target.result;
            const rawBase64 = rawDataUrl.split(',')[1];
            window.uploadedBase64[boxId] = rawBase64;

            const box = document.getElementById(boxId);
            Array.from(box.children).forEach(child => child.style.display = 'none');
            
            const previewDiv = document.createElement('div');
            previewDiv.className = 'preview-container absolute inset-0 w-full h-full';
            previewDiv.innerHTML = `
            <img src="${rawDataUrl}" class="absolute inset-0 w-full h-full object-contain opacity-80 rounded-xl">
            <button onclick="removeImage(event, '${boxId}')" class="absolute top-2 right-2 bg-red-600 text-white w-8 h-8 rounded-full flex items-center justify-center text-sm hover:bg-red-500 z-50 shadow-lg border border-white/20">✕</button>
            <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
            <span class="bg-black/70 px-4 py-2 rounded-lg text-[0.6rem] font-bold tracking-widest text-white shadow-lg uppercase">LOADED</span>
            </div>`;
            box.appendChild(previewDiv);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

// 🔥 EŞYA SİLME DÜZELTMESİ
function removeImage(event, boxId) {
    event.stopPropagation(); 
    event.preventDefault();
    playSound();
    delete window.uploadedBase64[boxId];
    const box = document.getElementById(boxId);
    const preview = box.querySelector('.preview-container');
    if(preview) preview.remove();
    Array.from(box.children).forEach(child => child.style.display = '');
    const fileInput = document.getElementById(boxId.replace('box', 'file'));
    if(fileInput) {
        fileInput.value = ""; 
        fileInput.type = "text"; 
        fileInput.type = "file"; 
    }
}

function activateFreeTrial() {
    playSound();
    const currentLang = document.getElementById('activeCode').innerText;
    let message = "Welcome! You have 1 Free Trial Credit. Proceeding to Dashboard...";
    if(currentLang === 'TR') message = "Hoş geldiniz! 1 Ücretsiz Deneme Krediniz tanımlandı. Panele yönlendiriliyorsunuz...";
    alert(message);
    document.getElementById('loginScreen').style.opacity = '0';
    setTimeout(() => {
        document.getElementById('loginScreen').style.display = 'none';
        document.getElementById('appContent').classList.add('active');
        enterDashboard('DESIGN');
    }, 500);
}

function selectBoardType(typeId) {
    playSound();
    window.currentBoardStyle = typeId;
    [1, 2, 3].forEach(id => {
        const btn = document.getElementById(`btnBord${id}`);
        if(btn) {
            if (id === typeId) {
                btn.className = "flex-1 bg-white border border-black/20 text-black py-3 rounded-xl text-[0.65rem] font-bold tracking-widest uppercase transition shadow-md";
                const span = btn.querySelectorAll('span')[1]; 
                if(span) span.classList.remove('opacity-70');
            } else {
                btn.className = "flex-1 bg-white/5 border border-white/10 text-white hover:bg-white/10 py-3 rounded-xl text-[0.65rem] font-bold tracking-widest uppercase transition";
                const span = btn.querySelectorAll('span')[1];
                if(span) span.classList.add('opacity-70');
            }
        }
    });
}

function exitDashboard() {
    playSound();
    document.getElementById('dashboard').classList.remove('active');
    document.getElementById('landing').classList.remove('hidden');
    closeRender();
    if (typeof closePresentation === 'function') closePresentation();
}

function b64toBlob(b64Data, contentType='', sliceSize=512) {
    const byteCharacters = atob(b64Data);
    const byteArrays = [];
    for (let offset = 0; offset < byteCharacters.length; offset += sliceSize) {
        const slice = byteCharacters.slice(offset, offset + sliceSize);
        const byteNumbers = new Array(slice.length);
        for (let i = 0; i < slice.length; i++) {
            byteNumbers[i] = slice.charCodeAt(i);
        }
        const byteArray = new Uint8Array(byteNumbers);
        byteArrays.push(byteArray);
    }
    return new Blob(byteArrays, {type: contentType});
}

// 🔥 8K UPSCALE MOTORU
async function ADEUL_UPSCALE(imageUrl) {
    try {
        const n8nUpscaleURL = "https://adeul-ia.app.n8n.cloud/webhook/upscale"; 
        const response = await fetch(n8nUpscaleURL, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ image: imageUrl })
        });
        
        if (!response.ok) throw new Error("N8N Webhook yanıt vermedi!");
        
        const data = await response.json();
        return data.output_url || data.output || data;
    } catch (error) {
        console.error("Upscale hatası:", error);
        throw error;
    }
}

// 🔥 RENDER MOTORU (4K ve Blob Entegrasyonu eklendi)
async function simulateAPIConnection(btnId, is8K = false) {
    playSound();
    const btn = document.getElementById(btnId);
    if (!btn || btn.disabled) return;
    btn.disabled = true;

    const originalText = btn.innerHTML;
    const promptInput = document.getElementById('promptArea');
    const userPrompt = promptInput && promptInput.value.trim() !== "" ? promptInput.value : "Modern architectural design";
    
    const activeLangCodeElement = document.getElementById('activeCode');
    const activeLangCode = activeLangCodeElement ? activeLangCodeElement.innerText : 'EN';

    btn.innerHTML = is8K ? '8K RENDER ALINIYOR...' : 'ADEUL AI GENERATING...';
    btn.classList.add('bg-blue-600', 'text-white', 'animate-pulse');

    let refImage = window.uploadedBase64['boxRef'] || window.uploadedBase64['boxScene'] || window.uploadedBase64['boxItem'] || window.uploadedBase64['boxDesign'] || Object.values(window.uploadedBase64)[0] || "";
    if (refImage) { window.uploadedBase64['boxRef'] = refImage; }

    let theSketchImage = "";
    if (Object.keys(window.uploadedBase64).length > 0) {
        theSketchImage = Object.values(window.uploadedBase64)[0];
    }

    const currentMenuTitle = document.getElementById('dashboardTitle').getAttribute('data-raw-title');
    
    let isSketchMode = "HAYIR";
    if ((currentMenuTitle === 'DESIGN' || currentMenuTitle === 'PLAN') && theSketchImage !== "") {
        isSketchMode = "EVET";
    }

    const payload = {
        action: 'generate',  
        prompt: userPrompt,
        isSketch: isSketchMode,
        sketchData: theSketchImage,
        images: window.uploadedBase64,
        language: activeLangCode,
        aspectRatio: window.currentRatio,
        imageSize: "4K",
        output_mime_type: "image/png"
    };

    try {
        const response = await fetch(window.CLOUDFLARE_URL, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json; charset=utf-8', 'Accept': 'application/json; charset=utf-8' },
            body: JSON.stringify(payload)
        });

        if (!response.ok) throw new Error(`Server Error: ${response.status}`);
        
        const arrayBuffer = await response.arrayBuffer();
        const decoder = new TextDecoder('utf-8');
        const data = JSON.parse(decoder.decode(arrayBuffer));
        
        let rawOutput = "";
        let result = Array.isArray(data) ? data[0] : data;

        if (result.output_url) {
            rawOutput = result.output_url;
        } else if (result.output && typeof result.output === 'string') {
            rawOutput = result.output;
        } else if (result.candidates && result.candidates[0].content && result.candidates[0].content.parts[0].inlineData) {
            rawOutput = result.candidates[0].content.parts[0].inlineData.data;
        } else if (result.url) {
            rawOutput = result.url;
        }

        if (rawOutput) {
            const imgElement = document.querySelector('#renderImgContainer img');
            if(imgElement) {
                let finalImage = "";
                
                if (rawOutput.startsWith('http')) {
                    imgElement.crossOrigin = "Anonymous";
                    imgElement.src = rawOutput;
                    finalImage = rawOutput;
                    showRenderScreen();
                } else {
                    let cleanBase64 = String(rawOutput).replace(/^data:image\/[a-z]+;base64,/, '').replace(/\s/g, '');
                    
                    const imageBlob = b64toBlob(cleanBase64, 'image/png'); 
                    finalImage = URL.createObjectURL(imageBlob);
                    
                    imgElement.src = finalImage;
                    showRenderScreen();
                }

                if (is8K) {
                    toggleUpscaleLoader(true); 
                    btn.innerHTML = '8K RENDER ALINIYOR...';
                    try {
                        const upscaledUrl = await ADEUL_UPSCALE(finalImage);
                        if(upscaledUrl) {
                            imgElement.crossOrigin = "Anonymous";
                            imgElement.src = upscaledUrl;
                        }
                    } catch (upscaleError) {
                        console.log("8K Upscale yapılamadı, orijinal render ekranda kaldı.", upscaleError);
                    } finally {
                        toggleUpscaleLoader(false); 
                    }
                }
            }
        }
    } catch (error) {
        console.error(error);
        alert("🚨 HATA!\nn8n bağlantısı başarısız.");
        closeRender();
    } finally {
        btn.innerHTML = originalText;
        btn.classList.remove('bg-blue-600', 'text-white', 'animate-pulse');
        btn.disabled = false;
        toggleUpscaleLoader(false); 
    }
}

// 🔥 İNDİRME MOTORU: CORS (Güvenlik) Engellerini Aşan %100 Çalışan Versiyon
async function saveRender(mode = 'renderImgContainer') {
    playSound();
    if (mode === 'renderImgContainer') {
        const imgElement = document.querySelector('#renderImgContainer img');
        if (!imgElement || !imgElement.src || imgElement.src.length < 500) { alert("Kaydedilecek bir görsel yok!"); return; }

        try {
            // Blob URL'leri (veya API linklerini) zorla indir
            const response = await fetch(imgElement.src);
            const blob = await response.blob();
            const blobUrl = window.URL.createObjectURL(blob);
            const link = document.createElement('a');
            link.href = blobUrl;
            link.download = `ADEUL_AI_RENDER_${new Date().getTime()}.png`;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
            setTimeout(() => window.URL.revokeObjectURL(blobUrl), 1000);
        } catch (err) {
            console.error("İndirme hatası, alternatif deneniyor:", err);
            // Fetch engellenirse (CORS) doğrudan yeni sekmede veya direkt tıklamayla aç
            const link = document.createElement('a');
            link.href = imgElement.src;
            link.download = `ADEUL_AI_RENDER_${new Date().getTime()}.png`;
            link.target = "_blank"; 
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }
    } 
    else if (mode === 'presentationOverlay') {
        const activeBoard = document.getElementById(`boardTemplate${window.currentBoardStyle}`);
        if(!activeBoard) return;

        const originalWidth = activeBoard.style.width;
        const originalHeight = activeBoard.style.height;
        const originalMaxWidth = activeBoard.style.maxWidth;
        const originalTransform = activeBoard.style.transform;
        
        activeBoard.classList.remove('transform', 'scale-[0.6]', 'lg:scale-100');
        const targetHeight = activeBoard.scrollHeight > 850 ? activeBoard.scrollHeight : 850;

        activeBoard.style.width = "1200px";
        activeBoard.style.height = targetHeight + "px"; 
        activeBoard.style.maxWidth = "none";
        activeBoard.style.transform = "none";
        
        const executeDownload = () => {
            html2canvas(activeBoard, { scale: 4, useCORS: true, scrollY: 0, backgroundColor: '#fdfdfd' }).then(canvas => {
                activeBoard.style.width = originalWidth;
                activeBoard.style.height = originalHeight;
                activeBoard.style.maxWidth = originalMaxWidth;
                activeBoard.style.transform = originalTransform;
                activeBoard.classList.add('transform', 'scale-[0.6]', 'lg:scale-100');
                
                const link = document.createElement('a');
                link.download = `ADEUL_AI_Board_${window.currentBoardStyle}_HQ_${new Date().getTime()}.png`;
                link.href = canvas.toDataURL('image/png', 1.0);
                link.click();
            });
        };

        if (typeof html2canvas === 'undefined') {
            const script = document.createElement('script');
            script.src = "https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js";
            script.onload = executeDownload;
            document.head.appendChild(script);
        } else {
            executeDownload();
        }
    }
}

document.addEventListener('click', function(e) {
    if (e.target.closest('button') || e.target.closest('a') || e.target.closest('.menu-item')) {
        playSound();
    }
});

function showRenderScreen() {
    const overlay = document.getElementById('renderOverlay');
    if(overlay) { overlay.classList.remove('hidden'); overlay.classList.add('flex'); setTimeout(() => overlay.style.opacity = '1', 50); }
}

function closeRender() {
    const overlay = document.getElementById('renderOverlay');
    if(overlay) {
        overlay.style.opacity = '0';
        setTimeout(() => overlay.classList.add('hidden'), 500);
        const imgElement = document.querySelector('#renderImgContainer img');
        if (imgElement) setTimeout(() => { imgElement.src = ""; }, 500);
    }
}

function showPresentationScreen() {
    const overlay = document.getElementById('presentationOverlay');
    if(overlay) { overlay.classList.remove('hidden'); overlay.classList.add('flex'); setTimeout(() => overlay.style.opacity = '1', 50); }
}

function closePresentation() {
    const overlay = document.getElementById('presentationOverlay');
    if(overlay) { overlay.style.opacity = '0'; setTimeout(() => overlay.classList.add('hidden'), 500); }
}

function toggleFullscreen() {
    document.getElementById('renderImgContainer').classList.toggle('fullscreen');
}

document.addEventListener('keydown', function(event) {
    if (event.key === 'Escape' || event.keyCode === 27) {
        const renderOverlay = document.getElementById('renderOverlay');
        const presentationOverlay = document.getElementById('presentationOverlay');
        const userPanel = document.getElementById('userSidePanel');
        const langMenu = document.getElementById('langDropdown');
        const dashboard = document.getElementById('dashboard');
        
        if (renderOverlay && !renderOverlay.classList.contains('hidden')) { closeRender(); } 
        else if (presentationOverlay && !presentationOverlay.classList.contains('hidden')) { if (typeof closePresentation === 'function') closePresentation(); } 
        else if (langMenu && !langMenu.classList.contains('hidden')) { langMenu.classList.add('hidden'); langMenu.classList.remove('flex'); } 
        else if (userPanel && userPanel.classList.contains('open')) { userPanel.classList.remove('open'); } 
        else if (dashboard && dashboard.classList.contains('active')) { exitDashboard(); }
    }
});